name: Email Step Demo

on:
  workflow_dispatch:   # allows manual trigger from GitHub UI

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2003992@ds.study.iitm.ac.in
        run: echo "Hello, world!" 

Add GitHub Actions workflow with email step name
